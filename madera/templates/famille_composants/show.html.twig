{% extends 'base.html.twig' %}

{% block title %}FamilleComposants{% endblock %}

{% block body %}
    <h1>FamilleComposants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille_composant.id }}</td>
            </tr>
            <tr>
                <th>Libelle_famille_composants</th>
                <td>{{ famille_composant.libelleFamilleComposants }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('famille_composants_index') }}">back to list</a>

    <a href="{{ path('famille_composants_edit', {'id': famille_composant.id}) }}">edit</a>

    {{ include('famille_composants/_delete_form.html.twig') }}
{% endblock %}
