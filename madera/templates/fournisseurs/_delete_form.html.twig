<form class="" method="post" action="{{ path('fournisseurs_delete', {'id': fournisseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" nd-inline-flex col-md-1ame="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fournisseur.id) }}">
    <button class="btn btn-danger ml-3"><i class="fas fa-trash"></i> Supprimer</button>
</form>
