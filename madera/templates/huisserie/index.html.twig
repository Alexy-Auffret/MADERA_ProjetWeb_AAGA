{% extends 'base.html.twig' %}

{% block title %}Huisserie index{% endblock %}

{% block body %}
    <h1>Huisserie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle_huisserie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for huisserie in huisseries %}
            <tr>
                <td>{{ huisserie.id }}</td>
                <td>{{ huisserie.libelleHuisserie }}</td>
                <td>
                    <a href="{{ path('huisserie_show', {'id': huisserie.id}) }}">show</a>
                    <a href="{{ path('huisserie_edit', {'id': huisserie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('huisserie_new') }}">Create new</a>
{% endblock %}
