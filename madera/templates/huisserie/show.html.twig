{% extends 'base.html.twig' %}

{% block title %}Huisserie{% endblock %}

{% block body %}
    <h1>Huisserie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ huisserie.id }}</td>
            </tr>
            <tr>
                <th>Libelle_huisserie</th>
                <td>{{ huisserie.libelleHuisserie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('huisserie_index') }}">back to list</a>

    <a href="{{ path('huisserie_edit', {'id': huisserie.id}) }}">edit</a>

    {{ include('huisserie/_delete_form.html.twig') }}
{% endblock %}
